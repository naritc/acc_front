export enum COLUMN_TYPE {
    TEXT = 'text',
    NUMBER = 'number',
    DATE = 'date',
    BOOLEAN = 'boolean',
    BUTTON = 'button',
    LINK = 'link',
    IMAGE = 'image',
    CUSTOM = 'custom',
    SELECT = 'select',
}
